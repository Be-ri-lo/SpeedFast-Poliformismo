package org.speedFast.util;

/**
 * Estados del pedido en la zona de carga.
 * Se usa enum para no escribir mal el nombre del estado.
 */
public enum EstadoPedido {
    PENDIENTE,
    EN_REPARTO,
    ENTREGADO
}
